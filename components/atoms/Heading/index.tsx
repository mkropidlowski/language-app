import { FC } from "react"


const Heading: FC = (
    (
        {
            // propsy
            //heading
        }
    ) => (
        <button>

        </button>
    )
)
   


export default Heading