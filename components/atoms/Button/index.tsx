import { FC } from "react"


const Button: FC = (
    (
        {
            // propsy
            // button
        }
    ) => (
        <button>

        </button>
    )
)
   


export default Button