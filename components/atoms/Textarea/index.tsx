import { FC, HTMLProps } from "react";
import style from "./textarea.module.scss";

interface Props {}

const Textarea: FC<Props & HTMLProps<HTMLTextAreaElement>> = ({}) => {
    return <div></div>;
};

export default Textarea;
